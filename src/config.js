/* Configuration variables - adjust to your deployment and/or region */

// MAP EXTENT
var startX = 20; // X coordinate of initial map center, in decimal degrees
var startY = 50; // Y coordinate of initial map center, in decimal degrees
var startZ = 15; // initial map zoom level

// TILES URL

var vtileUrl = "http://127.0.0.1/vtile/{z}/{x}/{y}";

